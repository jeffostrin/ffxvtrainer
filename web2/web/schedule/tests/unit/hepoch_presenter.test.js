'use strict';

const HepochPresenter = require('../../hepoch_presenter')

const assert = require('chai').assert;

describe('Hepoch Presenter', function () {

  it('handles single event data', async () => {
    var eventData = {
      "Monster": 250
    }
  });

  it('handles two event data', async () => {

  });

  it('handles three event data', async () => {

  });

});
